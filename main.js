// JS

// INIT ALT
/*var altParent = document.getElementById("pfdAltWrapper");

var alt20 = document.createElement("div");
var alt100 = document.createElement("div");

alt20.className = "pfdAlt alt20";
alt100.className = "pfdAlt alt100";

// Every fivehundred
var start = -100;
for(i = 0; i < 30; i++) { // 7000

	// Every hundred
	for(ib = 0; ib < 5; ib++) {
		if(i == 4) {
			altParent.appendChild(alt100).innerHTML = "<span>" + start + 100 + "</span>";
			break;
		}
		altParent.appendChild(alt20);
	}

}
 */

 